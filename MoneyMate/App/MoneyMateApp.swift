import SwiftUI

/// MoneyMate — iOS 26 Liquid Glass 记账应用骨架。
/// 部署目标 iOS 26.0+；仅使用 Apple 官方 SwiftUI API，无任何第三方依赖。
@main
struct MoneyMateApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
